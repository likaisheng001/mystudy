1、请总结静态代理和动态代理的根本区别。
静态代理属于硬编码，不灵活，不能做到适配，扩展性不好，动态代理扩展性好。
静态代理，代理类和被代理类需要实现相同的接口，且被代理类若是要增加一个方法，需要同时修改代理类和被代理类，代理类和被代理类耦合性高。
动态代理，只管维护对被代理类的控制和扩展。如果代理类调整了代码，不必修改被代理类；被代理类调整了代码，也不必修改代理类，耦合性低。

备注：凡是动态代理能办到的，静态代理也能办到，只是不太灵活而已。

